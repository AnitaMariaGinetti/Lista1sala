valor = float(input("Entre com o valor: "))

taxa = float(input("Entre com a taxa: "))

tempo = float(input("Entre com o período de tempo: "))

prestacao = valor + (valor * (taxa/100) * tempo)

print("O valor da prestação será de: ", prestacao)
